import 'package:flutter/material.dart';

class DetailVertical extends StatelessWidget {
  const DetailVertical({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
