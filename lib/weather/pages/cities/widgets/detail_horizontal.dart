import 'package:flutter/material.dart';

class DetailHorizontal extends StatelessWidget {
  const DetailHorizontal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
